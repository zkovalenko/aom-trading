{
  "name": "aom-trading-monorepo",
  "version": "1.0.0",
  "description": "AOM Trading Platform",
  "scripts": {
    "build": "cd backend && npm install && npm run build",
    "start": "cd backend && npm start",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd website && npm start"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "form-data": "^4.0.4",
    "mailgun.js": "^12.1.0"
  }
}
